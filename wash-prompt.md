# 洗課表 Prompt（Claude / GPT 通用）

> **v3 · 2026-09-20。** 併入 chat 版 v2 的結構改良（五問、單雙週兩種模式、PDF 字體缺陷、地點字樣過濾），
> 並把 v2 漏掉、但實測證明最關鍵的兩條規則補回來（對開科目跟著老師走、撞課檢查要排除 `teacher:"—"`）。

把下面整段貼給 Claude 或 GPT，連同你們學校的**班級課表 PDF** 一起丟進去。它會先跟你確認幾件事，再產出「調課雷達」能吃的 JSON。

---

你是我的課表資料助手。我會給你一份**班級課表** PDF（一頁一班），請幫我轉成一個 JSON 陣列，之後要餵給一個叫「調課雷達」的網頁工具。

## 輸出格式

整份是一個陣列，每一「格課」是一筆物件：

```json
{ "cls": "501", "day": 1, "period": 1, "subject": "英語文", "teacher": "林安" }
```

- `cls`：班級，字串。
- `day`：星期，1–5 對應 一～五。
- `period`：節次，數字 1–8。
- `subject`：科目名，照課表寫。**格子裡附帶的地點標註（教室、操場、講堂、實驗室…）要濾掉，不算進 subject。**
  ⚠️ 只濾掉「獨立的地點標註」，**不要動科目名稱本身**——例如「電腦繪圖與設計」是完整科名，不可以被砍成「繪圖與設計」。拿不準就保留原字串並列出來問我。
- `teacher`：老師姓名。
- `fixed`：**選填**，全校固定時段、或跨班跑班／同格多老師分組的課才加，值為 `true`（見下）。
- `flex`：**選填**，彈性學習／自主學習類的課加 `true`（見下）。
- `week`：**選填**，隔週輪替的課加 `"單"` 或 `"雙"`（見下）。

只輸出 JSON，不要多餘說明文字。空堂／沒有課的格子**不要**產生任何一筆。

## ⚠️ 最重要的一條規則（最容易錯，請先讀）

**一個格子展開成多筆時，每一筆的 `subject` 要跟著「那位老師」走，不要每筆都照抄格子上的那串字。**

班級課表的格子只有一格空間，常常只寫得下其中一科，但實際上兩位老師教的是不同科：

- **對開輔導課**：格子寫「公民輔導」，實際上是歷史老師上單週歷史輔導、公民老師上雙週公民輔導。
- **分組／跑班課**：格子寫「本土語文」，實際上是一位老師上客家語、另一位上排灣族語。

判斷方式：看該位老師在別的班／別節上的正課是什麼，用他的本科回填。**判斷不出來的，把那格列出來問我，不要兩筆都填同一科。**
（實測一份真實 18 班課表時，13 組對開輔導課 **13/13 全部**是兩科不同；2 組本土語文也都是兩科不同。兩筆同科反而才是異常。）

## 動手前，先問我這幾件事（逐項問，不要跳過）

1. **哪些課名算「固定節次」**（要標 `fixed: true`）？常見是班週會、朝會、週會、社團活動。這些是全校綁死、不可對調的時段，雷達要自動排除。

2. **有沒有「沒寫老師」的格子**？這兩種要分開處理，不要一律填 `—`：
   - **「社團活動」**沒寫老師 → `teacher` 填 `"—"`、標 `fixed: true`。
   - **「班週會」**沒寫老師 → `teacher` 填**該班導師姓名**（課表 header 通常會列導師）、標 `fixed: true`。這樣導師在自己的課表才看得到這節。
   不要漏掉、也不要瞎編老師。

3. **有沒有「同一格由多位老師分組上課」的課？**（常見於多元選修、彈性充補、自然探究與實作、本土語文）
   處理方式固定是：**每位老師各自產生一筆，都標 `fixed:true`**（不可對調，但每位老師的課表都看得到自己那筆）。
   `subject` 記得各自跟著老師走（見上面那條規則）。
   ➕ 追問我一句：**這些課裡有沒有「跨班跑班」的？**（學生會跑到別班上課）跑班的課**即使格子裡只寫一位老師，也要標 `fixed:true`**——因為動它會牽連到其他班。實測那份課表裡，「多元選修」10 格只有一位老師，仍然全部是 `fixed`。

4. **有沒有「單／雙週隔週輪替」的課？**（常見於各科輔導課，尤其第八節）
   處理方式固定是：**兩位老師各自產生一筆，都可對調（不標 `fixed`），並加 `week:"單"` / `week:"雙"` 標記**。
   ⚠️ **這裡有兩種模式，長得很像，兩種都要能處理**：
   - **模式一：同班、同節，兩位老師輪流教**（同一 `cls+day+period`，兩筆 `teacher` 不同）。
   - **模式二：同一位老師、同一節，但兩週教的班不一樣**（同一 `teacher+day+period`，兩筆 `cls` 不同）——常見於「兩班互相支援」：A 班單週老師剛好是 B 班雙週老師。這種特別容易被誤判成「老師撞課」。

   ⚠️ 另外：**分組課（第 3 題）和隔週輪替（第 4 題）在 PDF 上長得幾乎一樣，都是「一格兩個老師」，這是整份轉檔最容易判錯的一項。** 分組是兩人同時上、不可調；輪替是各上一週、可以調。分不出來就問我。

5. **是否要化名**？如果我要匿名分享，請把所有老師名字換成穩定的化名（同一個真名固定對到同一個化名），並另外給我一張「真名 ↔ 化名」對照表。
   **如果我打算把工具 publish 成公開網頁連結，即使不化名，也請提醒我：對外連結固定放 demo／化名資料，真實資料改用工具內建的「匯入」私下分流，避免被搜尋引擎索引到。**

## 轉檔時請注意

- **一次只處理一個部**：只轉高中部的班級（通常 18 班），不要混進國中部。
- **代／兼等小字**：課表上「代」「兼」這類註記不影響格子本身，`subject`／`teacher` 照實際上課的人與科目填即可，不用另外造欄位。
- **連堂**：同一位老師、同一班、相鄰兩節同一科（例如第三、四節都是同班美術），照樣各產一筆，不用合併——雷達會自己判斷連堂。
- **同班多科**：同一位老師在同一班教兩種科目（例如導師教國文又教彈性），照實際各產各的筆數。
- **自主學習／彈性課程類的課**：加 `flex:true`（**不是** `fixed`）。這種課任課老師掛名但實際彈性運用，可不可以換由老師自己判斷，工具只會標示提醒、不會阻擋對調。常見課名：自主學習、彈性課程、高一/高三彈性-XX充補。
  （但如果同一格有兩位老師分組上，就回到第 3 題：標 `fixed`，不標 `flex`。）
- **PDF 文字層字體缺陷**：如果某位老師姓名裡出現問號、缺字框、或其他不像正常中文姓名的字元，**整批列出來問我是誰，不要照發音或字形猜一個字填上去**——這不是排版問題，是 PDF 文字層本身壞掉，連 PDF 自己的統計欄位也會是壞的。
- 看不清楚或拿不準的格子，**先列出來問我**，不要猜。

## 最後請幫我做這四個檢查

這四條的判斷邏輯要跟「調課雷達」網頁裡的檢查一致，不然洗出來看似乾淨、匯進去才跳警告。

1. **撞課檢查**：同一位老師不應該在同一個 `day`+`period` 出現兩筆。
   **例外（這三種都不算撞課，不要列出來）**：
   - 這幾筆都有 `week` 標記、且彼此值不同（單/雙各一筆）→ 合法的隔週輪替，含上面的模式二。
   - `teacher` 是 `"—"` 的固定活動佔位 → 全校 18 班同一節都是它，本來就會大量同節。
   - `fixed:true` 的固定時段。

2. **重格檢查**：同一個 `cls` 不應該在同一個 `day`+`period` 出現兩筆。
   **例外**：這幾筆**都**有 `fixed:true`（同格多老師分組課）、或**都**有 `week` 標記（隔週輪替）→ 合法的多筆。兩者都不成立時才列為真的重格。

3. **對開科目檢查**：每一組隔週輪替（有 `week`）與每一組同格分組課（皆 `fixed`），如果兩位老師的本科不同，`subject` 就不該相同。
   **列出所有「同一格兩筆 subject 一樣」的格子給我確認**——照實測經驗，這種幾乎都是抄錯。

4. **逐科逐師核對**：PDF 每頁通常附一張「科目＋老師＋時數」的統計表，**請用這張表逐列核對**，而不是用那個「總時數」數字。
   ⚠️ **「總時數」算的是格數，不是筆數**——同格多老師分組課在它眼裡只算一格，所以它一定會比你洗出來的筆數少。
   （實測：某班總時數寫 35，但統計表加起來 38，我們洗出來也是 38；差的 3 正好是展開的分組課。）
   統計表則是逐位老師分開列的，加總等於展開後的筆數，還能順便驗證「哪位老師教哪一科」——**對開輔導課的科目該掛誰，看這張表就有答案，不用用猜的**。
   ⚠️ 如果 PDF 文字層有字體缺陷（見上），它的統計表也可能跟著壞，核不上時要一起考慮這個可能。

檢查沒問題後，把最終 JSON 給我（可下載成 `schedule.json`）。我會拿去匯入「調課雷達」。

---

> 拿到 JSON 之後，在專案目錄再跑一次機器驗證（這支腳本實作的檢查跟工具本身一致，還多抓幾項工具看不到的）：
>
> ```bash
> node validate-schedule.mjs schedule.json
> node validate-schedule.mjs schedule.json --baseline 上學期的.json
> node validate-schedule.mjs schedule.json --pdf 班級課表.pdf   # 跟原始 PDF 逐科逐師對帳
> ```
>
> 最後那個 `--pdf` 最重要：前面的檢查只驗 JSON 自己一不一致，**只有跟 PDF 對帳才驗得到有沒有抄錯**。
