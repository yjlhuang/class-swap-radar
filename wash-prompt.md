# 洗課表 Prompt（Claude / GPT 通用）

把下面整段貼給 Claude 或 GPT，連同你們學校的**班級課表 PDF**一起丟進去。它會先跟你確認幾件事，再產出「調課雷達」能吃的 JSON。

---

你是我的課表資料助手。我會給你一份**班級課表** PDF（一頁一班），請幫我轉成一個 JSON 陣列，之後要餵給一個叫「調課雷達」的網頁工具。

## 輸出格式

整份是一個陣列，每一「格課」是一筆物件：

```json
{ "cls": "501", "day": 1, "period": 1, "subject": "英語文", "teacher": "林安" }
```

- `cls`：班級，字串。
- `day`：星期，1–5 對應 一～五。
- `period`：節次，數字 1–8。
- `subject`：科目名，照課表寫。
- `teacher`：老師姓名。
- `fixed`：**選填**，固定節次或「同格多位老師分組上課」的選修課才加，值為 `true`（見下）。
- `flex`：**選填**，彈性學習／自主學習等「換不換老師自己判斷」的課加 `true`。
- `week`：**選填**，隔週輪替課填 `"單"` 或 `"雙"`（見下）。

只輸出 JSON，不要多餘說明文字。空堂／沒有課的格子**不要**產生任何一筆。

## 動手前，先問我這幾件事（很重要）

1. **哪些課名算「固定節次」**（要標 `fixed: true`）？常見是班週會、朝會、週會、社團活動。這些是全校綁死、不可對調的時段，雷達要自動排除。請把我回答的課名都標 `fixed: true`。
2. **有沒有「沒寫老師」的格子**？例如有些學校星期五第六、七節寫「社團」但沒有老師名字——那通常代表**這兩節是固定的社團／班週會時間**（每週活動不一定）。遇到這種沒有老師的格子，`teacher` 填 `"—"`、並標 `fixed: true`，不要漏掉也不要瞎編老師。
3. **同一格有沒有寫到兩位老師**？高中常見兩種，處理方式不同，請分別問我：
   - **分組選修**（多元選修、彈性充補、自然探究等，兩位老師同時各帶一組）→ 每位老師**各產一筆**，兩筆都標 `fixed: true`。這樣兩人的課表都看得到這節，但雷達不會誤判成可對調。
   - **隔週輪替**（多為第八節輔導課，標「單」「雙」或「單週／雙週」）→ 每位老師**各產一筆**，各自加 `"week": "單"` 或 `"week": "雙"`，**不要**標 `fixed`。這種課是可以對調的，雷達只會提醒先確認是哪一週。
     ⚠️ **這種格子的 `subject` 要跟著「老師」走，不要照抄格子上的字。** 班級課表那格常常只寫其中一科（例如只寫「公民輔導」），但實際上是兩科對開——歷史老師那一週上歷史輔導、公民老師那一週上公民輔導。請用該老師在別處的正課判斷他的本科，兩筆各自填自己的科目；若判斷不出來，把該格列出來問我，不要兩筆都填同一科。
4. **是否要化名**？如果我要匿名分享，請你把所有老師名字換成穩定的化名（同一個真名固定對到同一個化名），並另外給我一張「真名 ↔ 化名」對照表，方便我自己看。

## 轉檔時請注意

- **一次只處理一個部**：只轉高中部的班級（通常 18 班），不要混進國中部。
- **代／兼等小字**：課表上「代」「兼」這類註記不影響格子本身，`subject`／`teacher` 照實際上課的人與科目填即可，不用另外造欄位。
- **連堂**：同一位老師、同一班、相鄰兩節同一科（例如第三、四節都是同班美術），照樣各產一筆，不用合併——雷達會自己判斷連堂。
- **同班多科**：同一位老師在同一班教兩種科目（例如導師教國文又教彈性），照實際各產各的筆數。
- 看不清楚或拿不準的格子，**先列出來問我**，不要猜。

## 最後請幫我做這三個檢查

1. **撞課檢查**：同一位老師不應該在同一個 `day`+`period` 出現兩筆。**例外**：隔週輪替（有 `week`）與 `teacher:"—"` 的固定活動佔位，這兩種同節多筆是正常的。若有其他情形，列出來。
2. **重格檢查**：同一個 `cls` 在同一個 `day`+`period` 不應有兩筆。**例外**：分組選修（皆 `fixed:true`）與隔週輪替（有 `week`）。若有其他情形，列出來。
3. **對開科目檢查**：每一組隔週輪替的兩筆，如果兩位老師的本科不同，`subject` 就不該相同。列出所有「兩筆同科但兩位老師本科不同」的格子給我確認。
4. **時數檢查**：每班洗出來的筆數，要對得起課表上的總時數（含被展開成多筆的分組／輪替課）。對不上就列出差在哪幾節。

檢查沒問題後，把最終 JSON 給我（可下載成 `schedule.json`）。我會拿去匯入「調課雷達」。
