---
name: class-schedule-wash
description: 把學校的班級課表 PDF 轉成「調課雷達」工具能用的 JSON。當老師上傳班級課表 PDF、或提到要把課表洗成資料、匯入調課／排課小工具、產生 schedule.json 時使用。會先與老師確認固定節次、沒寫老師的格子、同格多老師與單雙週輪替，再輸出經過撞課／重格／對開科目檢查的 JSON。
---

# 把班級課表 PDF 洗成調課雷達 JSON

> **v3 · 2026-09-20。** 已用一份真實 18 班課表（115-1 學期，714 筆，含同格多老師分組課、單雙週輪替輔導課、PDF 字體缺陷）跑過一輪，下面的規則與陷阱都是那次驗證後的結果。
> 本檔與專案內的 `wash-prompt.md` 是同一套規則的兩種包裝，改一邊要同步改另一邊。

## 目標

輸入：一份**班級課表** PDF（一頁一班，格子含科目＋老師）。
輸出：一個 JSON 陣列，每筆是一格課，格式如下，可下載成 `schedule.json` 供「調課雷達」網頁匯入。

```json
{ "cls": "501", "day": 1, "period": 1, "subject": "英語文", "teacher": "林安" }
```

欄位：`cls`(班級字串)、`day`(1–5)、`period`(1–8)、`subject`、`teacher`、`fixed`(選填 true)、`flex`(選填 true)、`week`(選填 `"單"`/`"雙"`)。
空堂不產生任何一筆。格子裡附帶的**地點標註**（教室、操場、講堂、實驗室等）剝離，不算進 `subject`；但**不要動科目名稱本身**（「電腦繪圖與設計」不可砍成「繪圖與設計」）。

## 為什麼用班級課表而非教師課表

班級課表以班為單位，天生只含高中部，不必判斷老師屬哪個部；教師課表會混進國中部的課。所以**一律要求班級課表**；若老師只有教師課表，說明差異並請他改提供班級課表。

## ⚠️ 核心規則：一格展開成多筆時，`subject` 跟著「老師」走

班級課表一格只寫得下一串字，但同一格可能有兩位老師教**不同科**。**不要每筆都照抄格子上的標籤**，要看該老師在別處的正課判斷他的本科，各自回填；判斷不出來就列出來問老師。

- **對開輔導課**：格子寫「公民輔導」，實際是歷史老師上單週歷史輔導、公民老師上雙週公民輔導。
- **分組／跑班課**：格子寫「本土語文」，實際是一位上客家語、另一位上排灣族語。

真實資料佐證：13 組對開輔導課 **13/13 全部兩科不同**，2 組本土語文亦然。**兩筆同科才是異常訊號。**
填錯的後果：老師在自己課表看到一門他根本沒教的科目，而且工具的「本科統計」也會跟著歪。

## 流程

1. **讀 PDF、盤點**：確認是班級課表、共幾班、節次與星期軸怎麼排。優先用 `pdfplumber` 的 `find_tables()`／`extract_text()` 讀表格與 header（導師姓名等），比純視覺辨識準確。若是掃描檔（非文字 PDF），先提醒可能需要人工校對。

2. **開工前，向老師確認五件事**（逐項問，不要跳過）：
   - 哪些課名算**固定節次**（標 `fixed:true`）——通常班週會、朝會、週會、社團活動。
   - 有沒有**沒寫老師**的格子——「社團活動」→ `teacher:"—"` + `fixed:true`；「班週會」→ `teacher` 填**該班導師姓名** + `fixed:true`（讓導師在自己課表看得到）。
   - 有沒有**同一格多位老師分組上課**（多元選修、彈性充補、自然探究與實作、本土語文）——每位老師各一筆、都標 `fixed:true`，`subject` 各自跟著老師走。
     ➕ 追問**有沒有跨班跑班**：跑班的選修課**即使格子只寫一位老師也要 `fixed:true`**（實測「多元選修」有 10 格單老師，仍全部 fixed）。
   - 有沒有**單／雙週隔週輪替**（常見第八節輔導課）——兩位老師各一筆、都可對調、加 `week:"單"`/`"雙"`、**不標 fixed**。兩種模式都要處理：
     (a) 同班同節兩位老師輪流（同 `cls+day+period`，`teacher` 不同）；
     (b) 同一老師同節但兩週教不同班（同 `teacher+day+period`，`cls` 不同，兩班互相支援）。(b) 特別容易被誤判成撞課。
   - 是否要**化名**——要的話建立穩定的真名↔化名對照（同名固定同化名），另附對照表給老師。若老師打算把工具 publish 成公開連結，即使不化名也提醒：對外連結固定放 demo／化名資料，真實資料改用「匯入」私下分流。

3. **轉檔**：逐班、逐格產生筆數，遵守下面的處理規則。拿不準的格子先列出來問，不要猜。

4. **自我檢查**（用程式跑，不要靠眼睛逐筆核）——**判斷標準至少要跟「調課雷達」網頁本身一樣嚴，否則洗出來看似乾淨、匯入才跳警告**：
   - **撞課**：同一 `teacher` 不應在同一 `day+period` 出現兩筆。例外：有 `week` 且彼此值不同（含模式 b）、`teacher:"—"` 的固定活動佔位、`fixed:true`。
     ⚠️ **漏掉 `teacher:"—"` 這條例外，每次洗檔都會噴出一大串假撞課**（18 班社團活動全在同一節）。
   - **重格**：同一 `cls` 不應在同一 `day+period` 出現兩筆。例外：**皆** `fixed:true`（分組課）或**皆**有 `week`（隔週輪替）。
   - **對開科目**：隔週輪替與同格分組課的兩筆，若兩位老師本科不同，`subject` 就不該相同。列出所有「同格兩筆 subject 相同」的格子請老師確認。
   - **覆蓋**：用 PDF 每頁附的「科目＋老師＋時數」統計表**逐列**核對，不要用「總時數」那個數字——**總時數算的是格數，同格多老師只算一格，一定會比筆數少**（實測某班總時數 35、統計表合計 38、洗出來也是 38）。統計表逐師分列，加總等於展開後的筆數，而且能直接讀出「哪位老師教哪一科」，**對開輔導課的科目歸屬看它就有答案**。若 PDF 有字體缺陷，統計表也可能跟著壞，核不上時要一併考慮。
   列出任何異常給老師確認。

5. **交付**：輸出最終 JSON。若老師手邊有專案目錄，請他再跑一次 `node validate-schedule.mjs schedule.json --pdf <原始課表.pdf>`（同一套檢查的機器版，並會把 PDF 每頁的統計表抽出來逐科逐師對帳——那是唯一驗得到「有沒有抄錯」的檢查）。接著說明如何匯入（「調課雷達」右上「換／匯入課表資料」→貼上或上傳；匯入後存在瀏覽器本機記憶，同一台裝置下次打開不用重新匯入）。

## 處理規則

- 一次只轉一個部（高中部，通常 18 班），不混國中部。
- 代／兼等小字不另立欄位，`subject`／`teacher` 照實際授課填。
- 連堂（同師同班相鄰同科）照樣各產一筆，不合併——工具自行判斷。
- 同班多科（同師在同班教兩科）照實際各產各筆。
- 自主學習／彈性課程類**單老師**的課加 `flex:true`（不影響對調判斷，只是提醒）；同一格若有兩位老師分組上，改標 `fixed:true`、不標 `flex`。
- 同格多老師分組課：每位老師各一筆＋`fixed:true`。
- 單雙週輪替課：各一筆＋可對調＋`week` 標記，模式 (a)(b) 都要處理。
- 只輸出 JSON，不夾雜說明（說明另外講）。

## 常見陷阱

- **同格兩位老師分兩種，判錯是最容易出錯的一項**：分組選修（同時上、**不可**調）vs 隔週輪替（各上一週、**可以**調）。在 PDF 上長得幾乎一樣。判錯會讓雷達整格失效或誤判可調。
- **對開輔導課／分組課的科目最容易錯**：見上面〈核心規則〉。`subject` 一律跟著老師的本科走。
- 沒寫老師的固定活動格 → 社團 `teacher:"—"`、班週會填導師姓名，勿漏勿造假。
- 隔週輪替的兩位老師**有可能兩週教到不同班**（模式 b）。照實各產一筆即可，工具會把該格疊成兩列顯示。
- **PDF 文字層字體缺陷**：老師姓名裡出現問號、缺字框等不像正常中文姓名的字元，是 PDF 本身文字層壞掉（連 PDF 自己的統計欄位也會壞），**整批列出來問老師本人是誰，不要用相近字硬猜**。
- 跨部老師在班級課表裡只出現高中部那幾節，屬正常；不要為了補滿而編造。
- 各校節次時間、早自習列、午休位置不同，只需正確對到 `period` 1–8 即可。
- **撞課檢查若沒排除 `week` 與 `teacher:"—"`，會產生大量假警報**，老師會養成「看到警示就關掉」的習慣，反而蓋掉真正的撞課。

## 完成後

可另外提供一段「給對方老師的調課訊息」範本，或引導老師直接用「調課雷達」內建的分享功能產生。
