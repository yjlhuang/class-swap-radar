---
name: class-schedule-wash
description: 把學校的班級課表 PDF 轉成「調課雷達」工具能用的 JSON。當老師上傳班級課表 PDF、或提到要把課表洗成資料、匯入調課／排課小工具、產生 schedule.json 時使用。會先與老師確認固定節次與沒寫老師的格子，再輸出經過撞課／重格檢查的 JSON。
---

# 把班級課表 PDF 洗成調課雷達 JSON

## 目標

輸入：一份**班級課表** PDF（一頁一班，格子含科目＋老師）。
輸出：一個 JSON 陣列，每筆是一格課，格式如下，可下載成 `schedule.json` 供「調課雷達」網頁匯入。

```json
{ "cls": "501", "day": 1, "period": 1, "subject": "英語文", "teacher": "林安" }
```

欄位：`cls`(班級字串)、`day`(1–5)、`period`(1–8)、`subject`、`teacher`、`fixed`(選填 true)、`flex`(選填 true)、`week`(選填 `"單"`/`"雙"`)。
空堂不產生任何一筆。

## 為什麼用班級課表而非教師課表

班級課表以班為單位，天生只含高中部，不必判斷老師屬哪個部；教師課表會混進國中部的課。所以**一律要求班級課表**；若老師只有教師課表，說明差異並請他改提供班級課表。

## 流程

1. **讀 PDF、盤點**：確認是班級課表、共幾班、節次與星期軸怎麼排。若是掃描檔（非文字 PDF），先提醒可能需要人工校對。
2. **開工前，向老師確認三件事**（逐項問，不要跳過）：
   - 哪些課名算**固定節次**（要標 `fixed:true`）——通常班週會、朝會、週會、社團活動。
   - 有沒有**沒寫老師**的格子——常見是「社團／班週會」不寫老師，代表該節是全校固定活動時段。這種 `teacher` 填 `"—"`、標 `fixed:true`。
   - 同一格**有沒有兩位老師**——分組選修（多元選修／彈性充補／自然探究）各產一筆並都標 `fixed:true`；隔週輪替輔導課各產一筆並加 `week:"單"`/`"雙"`、**不標** fixed（這種可以對調），且 `subject` 要跟著老師的本科走、不要照抄格子上的單一標籤。
   - 是否要**化名**——要的話建立穩定的真名↔化名對照（同名固定同化名），另附對照表給老師。
3. **轉檔**：逐班、逐格產生筆數。遵守下面的處理規則。拿不準的格子先列出來問，不要猜。
4. **自我檢查**（用程式或逐筆核對）：
   - 撞課：同一 `teacher` 不應在同一 `day+period` 出現兩筆。例外：有 `week` 的隔週輪替、`teacher:"—"` 的固定活動佔位。
   - 重格：同一 `cls` 不應在同一 `day+period` 出現兩筆。例外：分組選修（皆 `fixed:true`）、隔週輪替（有 `week`）。
   - 覆蓋：班數、每班節數是否合理（總時數要對得起筆數，含展開成多筆的分組／輪替課）。
   - 對開科目：隔週輪替的兩筆若兩位老師本科不同，`subject` 就不該相同。列出所有同科但異本科的格子。
   列出任何異常給老師確認。
5. **交付**：輸出最終 JSON，說明如何匯入（「調課雷達」右上「換／匯入課表資料」→貼上或上傳）。

## 處理規則

- 一次只轉一個部（高中部，通常 18 班），不混國中部。
- 代／兼等小字不另立欄位，`subject`／`teacher` 照實際授課填。
- 連堂（同師同班相鄰同科）照樣各產一筆，不合併——工具自行判斷。
- 同班多科（同師在同班教兩科）照實際各產各筆。
- 只輸出 JSON，不夾雜說明（說明另外講）。

## 常見陷阱

- 沒寫老師的固定活動格 → `teacher:"—"` + `fixed:true`，勿漏勿造假。
- **同格兩位老師分兩種**：分組選修（同時上、不可調）vs 隔週輪替（各上一週、可調）。判錯會讓雷達整格失效或誤判可調，是最容易出錯的一項。
- 隔週輪替的兩位老師**有可能兩週教到不同班**（例如同一節單週教 501、雙週教 503）。照實各產一筆即可，工具會把該格疊成兩列顯示。
- **對開輔導課的科目最容易錯**（2026-09-15 真資料 13/13 格全中）：班級課表那格只寫一科，兩位老師卻是兩科對開。若把兩筆都填成格子上那一科，老師會在自己課表看到自己根本沒教的科目。`subject` 一律跟著老師的本科走。
- 跨部老師在班級課表裡只出現高中部那幾節，屬正常；不要為了補滿而編造。
- 各校節次時間、早自習列、午休位置不同，只需正確對到 `period` 1–8 即可。

## 完成後

可另外提供一段「給對方老師的調課訊息」範本，或引導老師直接用「調課雷達」內建的分享功能產生。
